<div class="fractal-canvas">
  <div class="ui left internal rail">
    <div class="ui segment fractal-menu">
      <p>{{precision-slider precision=precision updateHandler=(action "initCanvas")}}</p>
      <p>{{color-list color=color updateHandler=(action "initCanvas")}}</p>
    </div>
  </div>
  <canvas id="canvas"></canvas>
</div>
{{yield}}